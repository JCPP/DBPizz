{% extends 'base.twig' %}

{% block subtitle %}- Ordini{% endblock %}

{% block content %}
	{% include 'formOrdiniRicerca.twig' %}
	<a href="{{ app.urlFor("OrdiniAdd") }}">Aggiungi un ordine</a></br>
	{% if ordini %}
		<table border="1px">
			<thead>
				<tr>
					<th>ID Cliente</th>
					<th>ID Prodotto</th>
					<th>Data</th>
				</tr>
			</thead>
			<tbody>
				{% for ordine in ordini %}
					<tr>
						<td>{{ ordine.IDCliente }}</td>
						<td>{{ ordine.IDProdotto }}</td>
						<td>{{ ordine.DataOrdine }}</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	{% else %}
		Nessun ordine effettuato.
	{% endif %}
{% endblock %}