{% extends 'base.twig' %}

{% block subtitle %}- Menu{% endblock %}

{% block content %}
	<a href="{{ app.urlFor("PizzerieAdd") }}">Torna indietro</a></br>
	{% if appartiene %} //controllare condizione
		<table>
			<thead>
				<tr>
					<th>Nome</th>
					<th>Prezzo</th>
					<th>Aggiungi all'ordine</th>
				</tr>
			</thead>
			<tbody>
				{% for prodotto in appartiene %} //controllare condizione
					<tr>
						<td>{{ prodotto.Nome_Pr }}</td>
						<td>{{ prodotoo.Prezzo_Pr }}</td>
						<td>{{<a href="{{ app.urlFor("ordine") }}"</a>}}
					</tr>
				{% endfor %}
			</tbody>
		</table>
	{% else %}
		Nessun prodotto per questa pizzeria.
	{% endif %}
{% endblock %}